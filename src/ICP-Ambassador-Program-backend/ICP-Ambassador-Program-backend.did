service : {
    "create_user": (nat64, text, text, opt text, opt text, bool) -> ();
    "get_user": (nat64) -> (opt record {
        user_id: nat64;
        discord_id: text;
        username: text;
        referral_code: opt text;
        referred_by: opt text;
        joined_discord_server: bool;
    }) query;
    "update_user": (nat64, text, text, opt text, opt text, bool) -> ();
    "delete_user": (nat64) -> ();
}
